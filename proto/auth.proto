syntax = "proto3";

package auth;

// AuthService provides authentication operations
service AuthService {
    // Authenticate verifies a user token and returns the authenticated user
    rpc Authenticate(AuthenticateRequest) returns (User);
}

message AuthenticateRequest {
    string token = 1;
}

message User {
    int32 id = 1;
    string email = 2;
}